import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appRendoAlumno',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
